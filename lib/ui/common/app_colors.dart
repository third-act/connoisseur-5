import 'package:flutter/material.dart';

/// Connoisseur brand tokens — light paper + gold.
class AppColors {
  AppColors._();

  static const accent = Color(0xFFA7945D);
  static const accentSoft = Color(0x1AA7945D);
  static const cta = Color(0xFFD28E3D);

  static const money = Color(0xFF1B7A4E);

  static const bg = Color(0xFFEAE6DC);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceRaised = Color(0xFFFFFEFC);
  static const hairline = Color(0x0F000000);
  static const ink = Color(0xFF000000);
  static const inkMuted = Color(0xFF777777);
  static const inkFaint = Color(0xFF999999);

  static const shadow = Color(0x14070A12);
}
